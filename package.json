{
  "name": "elm-google-extension",
  "version": "1.0.0",
  "description": "This project is bootstrapped with [Create Elm App](https://github.com/halfzebra/create-elm-app).",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "elm make src/Main.elm --output=dist/index.html && cp manifest.json src/*.js dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "uji",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-3"
  }
}
